---
name: prepare-daily
description: >
  Подготовка брифинга перед дейлик-митингом: сбор открытых поручений, повторяющихся
  блокеров, вчерашних планов и порядка выступлений. Используй этот скилл когда
  пользователь говорит "подготовь дейлик", "что у нас открыто", "брифинг перед
  стендапом", "prepare daily", "что осталось с прошлого дейлика", "открытые action items",
  "подготовка к встрече".
---

# Prepare Daily — подготовка к дейлику

## Что делает скилл

Запускает `scripts/prepare_daily.py` и выдаёт тимлиду готовый брифинг:
1. Порядок выступлений (ротация)
2. Модули команды (справочно: кто в чём разбирается)
3. OKR at risk (team-уровень)
4. Релизы близко (KR, у которых dev_freeze в ближайшие 21 день)
5. Что каждый планировал вчера (для сверки)
6. Незакрытые action items из предыдущих дней
7. Повторяющиеся блокеры
8. Контекст последнего дейлика

Секция «Модули» берётся из `modules` в `teams/<team>/team.yaml` и нужна,
чтобы быстро вспомнить, кого тянуть за ревью/консультацией по конкретной области.

## Использование

### Шаг 1: Определить команду и дату

- `team` — ОБЯЗАТЕЛЬНО. Спроси, если не указана.
- `date` — по умолчанию сегодня.

### Шаг 2: Запустить скрипт

```bash
python3 scripts/prepare_daily.py --team TEAM_NAME [--date YYYY-MM-DD] [--history N]
```

Параметр `--history` — сколько прошлых митингов сканировать (по умолчанию 7).

### Шаг 3: Показать результат

Выведи брифинг пользователю. Обрати внимание на:

- Просроченные action items (`due_date < сегодня`) — выдели отдельно
- Повторяющиеся блокеры — предложи вынести в отдельное обсуждение
- Если открытых items > 5 — предложи приоритизировать

## Зависимости

- `scripts/prepare_daily.py`
- Наличие `meetings/` с обработанными `structured.json`
