[project]
name = "meeting-review"
version = "0.1.0"
description = "Daily standup automation: transcription → structured data → validation → reviews → insights"
requires-python = ">=3.10"
dependencies = [
 "pyyaml>=6.0",
 "jsonschema>=4.17",
]
